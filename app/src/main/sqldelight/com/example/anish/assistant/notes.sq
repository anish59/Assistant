CREATE TABLE notes
(
	NoteId	INTEGER NOT NULL PRIMARY KEY,
	Title	TEXT NOT NULL,
	Description	TEXT
);

select_all_notes:
select * from notes;

select_all_notes_by_noteid:
select * from notes where NoteId=?;